using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Tilemaps;

public class PlantCollections : MonoBehaviour
{
    /// <summary>
    /// This will handle what plants there are and where you can get them from
    /// Adding new items at load time may be procesesed here
    /// </summary>
    public PlantBase[] plantBases;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
